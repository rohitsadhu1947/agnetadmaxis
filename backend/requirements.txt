fastapi==0.109.0
uvicorn[standard]==0.27.0
sqlalchemy==2.0.25
pydantic==2.5.3
pydantic-settings==2.1.0
anthropic==0.43.0
python-telegram-bot==21.0
httpx==0.26.0
python-dotenv==1.0.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9  # required by FastAPI File() for /broadcasts/upload-attachment

# Database drivers
psycopg2-binary==2.9.9    # PostgreSQL (Neon DB in production)
aiosqlite==0.19.0          # SQLite (local dev fallback)

# Voice call demo (POC) - LiveKit WebRTC + Sarvam AI for Hindi/English STT+TTS
livekit>=1.0.0,<2.0.0          # LiveKit Python SDK (server-side room client)
livekit-api>=1.0.0,<2.0.0      # LiveKit REST API + AccessToken JWT minting
websockets>=12.0               # WebSocket support for transcript relay
